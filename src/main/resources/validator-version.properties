# OpenAPI Validator Version Information
# This file is filtered by Maven during build to inject actual version values
validator.version=${project.version}
validator.name=${project.artifactId}
validator.build.timestamp=${build.timestamp}
